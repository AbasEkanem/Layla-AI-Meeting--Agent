"""Milo's tools — Jira (create, assign, label, link epics).

Exposed as MILO_TOOLS and referenced by agent.py's spec. Empty for now —
structure pass.
"""

from langchain_core.tools import BaseTool

# TODO: add Jira tools (create issue, assign, set labels, link to epic).
MILO_TOOLS: list[BaseTool] = []
