"""Dex's tools — Gmail, Google Forms, Google Drive.

Exposed as DEX_TOOLS and referenced by agent.py's spec. Empty for now —
structure pass.
"""

from langchain_core.tools import BaseTool

# TODO: add Gmail (draft, send), Google Forms (create), and Drive tools.
DEX_TOOLS: list[BaseTool] = []
