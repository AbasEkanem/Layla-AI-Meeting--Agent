"""Vera's tools — Slack (messages, DMs, threads, Block Kit).

Exposed as VERA_TOOLS and referenced by agent.py's spec. Empty for now —
structure pass.
"""

from langchain_core.tools import BaseTool

# TODO: add Slack tools (post message, DM, reply-in-thread, Block Kit publish).
VERA_TOOLS: list[BaseTool] = []
